#include "monty.h"
/**
* _stack - prints the top of stack
* @stack: head
* @line_number: line number
* Return: void
*/

void _queue(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	dt_format = QUEUE;
}
